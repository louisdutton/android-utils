<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Repository metadata and APK hashes are authenticated by the pinned signing key. -->
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>
